ডে ১: জাভাস্ক্রিপ্ট পরিচিতি ও ভেরিয়েবল - বিস্তারিত নোট 📝

১. জাভাস্ক্রিপ্ট আসলে কী? (What is JavaScript?)

সহজ কথায়, জাভাস্ক্রিপ্ট হলো ইন্টারনেটের ভাষা। আমরা যখন কোনো ওয়েবসাইট দেখি, তখন মূলত তিনটি জিনিস কাজ করে। শিক্ষার্থীদের বোঝানোর জন্য আমরা একটি মানুষের শরীরের বা বাড়ি তৈরির উদাহরণ দিতে পারি:

HTML (Structure): এটি হলো কঙ্কাল বা বাড়ির ইটের গাঁথুনি। এটি ওয়েবসাইটের কাঠামো তৈরি করে।

CSS (Style): এটি হলো শরীরের চামড়া/পোশাক বা বাড়ির রঙ ও ডিজাইন। এটি ওয়েবসাইটকে সুন্দর করে।

JavaScript (Functionality): এটি হলো মানুষের ব্রেইন বা নার্ভাস সিস্টেম, অথবা বাড়ির বিদ্যুৎ সংযোগ। এটি ওয়েবসাইটকে কাজ করতে সাহায্য করে (যেমন: বোতাম চাপলে লাইট জ্বলা, ফর্মে ক্লিক করা ইত্যাদি)।

কেন শিখব? বিশ্বের ৯৮% ওয়েবসাইটে জাভাস্ক্রিপ্ট ব্যবহার করা হয়। এটি ছাড়া আধুনিক ইন্টারনেট অচল।

২. কোড কোথায় লিখব? (Environment Setup)

শুরুতে আমাদের কোনো ভারী সফটওয়্যার লাগবে না। প্রতিটি ব্রাউজারেই (Chrome, Firefox) জাভাস্ক্রিপ্ট ইঞ্জিন থাকে।

ব্রাউজার কনসোল (Console): এটি ব্রাউজারের একটি জায়গা যেখানে আমরা সরাসরি জাভাস্ক্রিপ্ট লিখে টেস্ট করতে পারি।

খোলার নিয়ম: ব্রাউজারে রাইট ক্লিক করুন -> Inspect সিলেক্ট করুন -> Console ট্যাবে যান।

প্রথম কোড: console.log("Hello World"); লিখুন এবং Enter চাপুন।

৩. ভেরিয়েবল: তথ্যের পাত্র (Variables In-Depth)

প্রোগ্রামিংয়ের সবচেয়ে গুরুত্বপূর্ণ কনসেপ্ট হলো ভেরিয়েবল।

বাস্তব উদাহরণ: মনে করুন আপনার রান্নাঘরে মশলা রাখার জন্য ৩টি কৌটা আছে। ১. একটি কৌটায় "চিনি" রাখলেন। ২. অন্যটিতে "লবণ" রাখলেন। ৩. কৌটার গায়ে লেভেল লাগিয়ে দিলেন যাতে চেনা যায়।

এখানে:

কৌটা = Variable

চিনি/লবণ = Data (Value)

লেভেল = Variable Name

ভেরিয়েবল লেখার নিয়ম (Syntax):

জাভাস্ক্রিপ্টে ভেরিয়েবল ডিক্লেয়ার করার জন্য বর্তমানে আমরা let এবং const ব্যবহার করি। (পুরানো কোডে var দেখা যায়, তবে নতুনদের var ব্যবহার না করাই ভালো)।

ক. let (পরিবর্তনযোগ্য)

যখন আমরা জানি যে কৌটার ভেতরের জিনিস ভবিষ্যতে পরিবর্তন হতে পারে।

let score = 10;
console.log(score); // দেখাবে: 10

score = 20; // মান পরিবর্তন করলাম
console.log(score); // এখন দেখাবে: 20


খ. const (অপরিবর্তনযোগ্য / Constant)

যখন আমরা এমন কিছু রাখব যা কখনো বদলাবে না। যেমন: আপনার জন্ম তারিখ।

const pi = 3.1416;
console.log(pi);

// pi = 4; // এটি করলে এরর (Error) দিবে, কারণ const পরিবর্তন করা যায় না।


নামকরণের নিয়ম (Naming Convention):

১. ভেরিয়েবলের নামে কোনো স্পেস (ফাঁকা জায়গা) রাখা যাবে না। (যেমন: my name ভুল, my_name বা myName সঠিক)। ২. সংখ্যা দিয়ে নাম শুরু করা যাবে না। (1name ভুল, name1 সঠিক)। ৩. জাভাস্ক্রিপ্টে সাধারণত camelCase ব্যবহার করা হয়। (যেমন: studentAge, totalPrice, firstName)।

৪. কমেন্টস (Comments)

কোডের মধ্যে নোট লিখে রাখার নামই কমেন্ট। এটি ব্রাউজার পড়ে না, শুধু মানুষের বোঝার জন্য রাখা হয়।

// এটি এক লাইনের কমেন্ট (Single line comment)

/* এটি অনেক লাইনের কমেন্ট 
   (Multi-line comment)
   এখানে যা লিখব কোডে প্রভাব ফেলবে না।
*/


৫. প্র্যাকটিস টাস্ক (বিস্তারিত)

শিক্ষার্থীদের নিচের কাজগুলো ক্লাসে করতে দিন:

টাস্ক ১: কনসোলে নিজের নাম প্রিন্ট করা। টাস্ক ২: const ব্যবহার করে নিজের জন্মসাল (birthYear) স্টোর করা। টাস্ক ৩: let ব্যবহার করে বর্তমান বয়স (currentAge) স্টোর করা। টাস্ক ৪: ভুল করে const ভেরিয়েবলের মান পরিবর্তন করার চেষ্টা করা এবং কনসোলে কী এরর আসে তা দেখা।

৬. সাধারণ ভুলসমূহ (Common Mistakes to Avoid)

console.log এর ভেতরে স্ট্রিং (লেখা) লিখতে হলে অবশ্যই কোটেশন "" বা '' দিতে হবে।

ভুল: console.log(Hello);

সঠিক: console.log("Hello");

সেমিকোলন ; দেওয়া ভালো অভ্যাস, যদিও জাভাস্ক্রিপ্টে এটি বাধ্যতামূলক নয়।

বড় হাতের এবং ছোট হাতের অক্ষর আলাদা অর্থ বহন করে (Case Sensitive)। Name এবং name দুটি আলাদা ভেরিয়েবল।